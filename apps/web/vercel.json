{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
  "buildCommand": "cd ../.. && pnpm turbo run build --filter=web",
  "crons": [
    {
      "path": "/api/cron/abandoned-designs",
      "schedule": "0 9 * * *"
    }
  ]
}
