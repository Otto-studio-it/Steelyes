---
title: Client answers — Marius, 2026-08-11
description: Batch CA-14…CA-17 — railhead/dog-bar auto density, circles as option, Design 2D shows model picker only (no finials drawn)
owner: Ruben
status: ACTIVE
last_updated: 2026-08-11
source: WhatsApp replies from Marius to Ruben lock questions (phase 0 decorative polish)
related: docs/frontend/GO_LIVE_LIMITATIONS.md, docs/db/RAILHEADS_TBD.md, packages/gate-engine/src/rules/geometry.ts
---

# Client answers — Marius, 2026-08-11

Lock batch for decorative polish after final-delivery feedback (arches / capuri / dog bars / cercuri). Ruben asked four yes/no questions; Marius answered the same day.

**Net effect of this batch:** closes `open.railhead_count_rule` and `open.dog_bars_count_rule` for *layout density*; confirms circles as a product option; locks Design-tab policy for railheads (catalogue select only, not drawn on 2D). Does **not** close full railhead SKU list / final unit prices (`open.railhead_variants` still open).

---

## Summary

| Id | Subject | Confidence | Closes blocking question | Needs code change |
|---|---|---|---|---|
| [CA-14](#ca-14--railheads-auto-one-per-picket-bay) | Railheads auto density (top / middle / dog) | `CONFIRMED` | `open.railhead_count_rule` | Yes — engine count + remove qty UI |
| [CA-15](#ca-15--dog-bars-auto-density) | Dog bars auto density | `CONFIRMED` | `open.dog_bars_count_rule` (layout) | Yes — remove qty UI; keep £ pricing survey-flagged |
| [CA-16](#ca-16--circles-as-onoff-band-option) | Circles = on/off band option | `CONFIRMED` | partial `open.circles_count_rule` / `deco.circles` | Yes — option + masters/overlay |
| [CA-17](#ca-17--design-2d-railhead-model-only) | Design 2D = choose model only | `CONFIRMED` | supersedes earlier ~70/80% draw ask for Design tab | Yes — no RH overlay on Design |

---

## Outbound lock questions (Ruben → Marius)

```txt
1. Capete (railheads) pe top, middle bar și dog bars — numărul e automat: câte unul în fiecare spațiu dintre bare. Clientul nu alege câte.
2. Dog bars — la fel, densitate automată (fără cantitate manuală).
3. Cercuri (circles) — le adăugăm ca opțiune da/nu, pe benzi (sus/jos), ca în pozele voastre.
4. În desenul 2D (Design) capetele nu apar pe poartă; se aleg doar modelul (ex. RH32) pentru preț/ofertă. Pe AR le putem arăta după. E ok așa?
```

---

## CA-14 — Railheads auto one per picket bay

### Verbatim (Romanian)

```txt
1 da
```

### Reading

Yes to: railheads on top, middle bar, and dog bars are counted automatically — **one in each space between bars/pickets**. The customer does **not** pick a quantity.

### Normalized rule

```txt
CONFIRMED — railhead_count_rule:
  For top_railheads, dog_bar_railheads, and any middle-bar railhead row:
  quantity = number of picket bays (one finial per inter-picket space).
  UI: model/SKU picker only; no manual quantity control.
```

### Downstream impact

- Replace provisional divisors in `packages/gate-engine/src/rules/geometry.ts` (`getExpectedTopRailheadCount`, `getExpectedDogBarRailheadCount`) with bay-based count from live picket layout.
- Configurator: hide qty steppers for railhead options.
- Pricing: `auto_qty × unit_price_gbp` still survey-flagged until SKU list signed.
- Docs: `docs/db/RAILHEADS_TBD.md`, `GO_LIVE_LIMITATIONS.md`.

---

## CA-15 — Dog bars auto density

### Verbatim (Romanian)

```txt
2 da
```

### Reading

Yes to: dog bars use automatic density — no manual quantity.

### Normalized rule

```txt
CONFIRMED — dog_bars layout density is automatic (same “fill every bay / space” principle as railheads).
Customer toggles dog_bars on/off only.
£75 FROM + £4.50 uplift pricing formula remains survey-flagged until a worked money example is signed (layout rule ≠ price formula).
```

### Downstream impact

- Remove dog-bar quantity UI; engine places bars by bay rule.
- Keep pricing disclaimer until money rule confirmed separately.

---

## CA-16 — Circles as on/off band option

### Verbatim (Romanian)

```txt
3da
```

### Reading

Yes to adding **circles (cercuri)** as an on/off option with bands (top/bottom), matching workshop photos.

### Normalized rule

```txt
CONFIRMED — deco.circles ships as boolean option.
Visual: horizontal circle / ring bands (typically upper + lower zones between rails).
Count inside a band follows the same auto bay-fill principle unless Marius later specifies otherwise.
```

### Downstream impact

- Add/enable `circles` (or equivalent) gate option in catalog + UI.
- Phase 1 asset: reusable circle-band SVG overlay; wire into Victorian masters.
- `open.circles_count_rule`: layout direction closed; exact £ formula still open if priced per piece.

---

## CA-17 — Design 2D railhead model only

### Verbatim (Romanian)

```txt
4 se alege doar modelul
```

### Reading

On question 4 (hide finials on Design 2D; pick model only for quote; AR later): Marius confirms **only the model is chosen** — i.e. Design does not need to draw each railhead on the gate silhouette.

### Normalized rule

```txt
CONFIRMED — Design tab policy:
  Railhead SKUs are selected in options (e.g. RH32, RH7…) for quote/BOM.
  Design 2D masters do NOT render per-picket finial overlays.
  AR / 3D may show finials later; not required for Design honesty.
This supersedes the earlier “~70/80% like photo on the drawing” ask for the Design tab only.
```

### Downstream impact

- Do **not** instance RH SVG overlays on Design masters.
- Keep the 6 RH SVG components for Figma catalog / future AR, not for Design paint.
- Phase 1 Figma work focuses on arched / dog_bars / middle_bar / circles masters — not RH placement on gates.

---

## Explicitly still open

| Question | Why still open |
|---|---|
| `open.railhead_variants` | Full sell list + live £ sign-off (61 SKUs provisional) |
| Dog-bar **money** formula (£75 / £4.50) | Layout closed; commercial worked example not re-confirmed here |
| Circles **unit price** | Option yes; £ per band or per circle not stated |
| Arched visual fidelity | Separate polish (Marius complaint) — not part of this yes/no lock |
