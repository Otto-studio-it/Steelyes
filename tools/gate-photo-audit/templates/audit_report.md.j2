# Gate audit: {{ gate_type }}

Generated by `tools/gate-photo-audit`. Use this to improve 2D rendering and 3D mesh fidelity.

## Summary

| Metric | Count |
|--------|------:|
| MATCH | {{ match_count }} |
| GAP | {{ gap_count }} |
| CONFLICT | {{ conflict_count }} |

**Vision confidence:** {{ "%.0f"|format(observed.confidence * 100) }}%

## Photos analyzed

{% for name in photo_names -%}
- `{{ name }}`
{% endfor %}

## Manifest

- Expected gate type: `{{ manifest.gate_type }}`
- Style hint: `{{ manifest.style_hint or "—" }}`
{% if manifest.known_dimensions_mm -%}
- Known dimensions: {{ manifest.known_dimensions_mm.width }} × {{ manifest.known_dimensions_mm.height }} mm
{% endif -%}
{% if manifest.notes -%}
- Notes: {{ manifest.notes }}
{% endif %}

## Observed topology

**Guess:** `{{ observed.gate_type_guess }}` · **Style:** `{{ observed.style_guess }}` · **Leaves:** {{ observed.leaf_count }}

### Parts

```json
{{ observed.topology | tojson(indent=2) }}
```

### Visible options

{% for key, enabled in observed.visible_options.items() -%}
- `{{ key }}`: {{ "yes" if enabled else "no" }}
{% endfor %}

## How it is built (observed)

{{ observed.composition_narrative.how_parts_connect }}

## Why it looks this way

{{ observed.composition_narrative.why_layout }}

### Distinctive features

{% for feature in observed.composition_narrative.distinctive_features -%}
- {{ feature }}
{% endfor %}

## Proportion observations

{% for key, value in observed.proportion_observations.items() -%}
- **{{ key }}:** {{ value }}
{% endfor %}

## Diff vs current configurator

| Area | Observed | Engine today | Status | Notes |
|------|----------|--------------|--------|-------|
{% for item in diff_items -%}
| {{ item.area }} | {{ item.observed }} | {{ item.engine_today }} | **{{ item.status }}** | {{ item.notes or "—" }} |
{% endfor %}

## Renderer recommendations (from analysis)

{% for rec in observed.renderer_recommendations -%}
- [ ] {{ rec }}
{% endfor %}

## Open questions for client / site measure

{% for q in observed.open_questions_for_client -%}
- [ ] {{ q }}
{% endfor %}

## Uncertainties

{% for u in observed.uncertainties -%}
- {{ u }}
{% endfor %}

---

_Next step: implement recommendations in `packages/gate-engine/src/rendering.ts` and `mesh/`, then re-run audit after new photos._
